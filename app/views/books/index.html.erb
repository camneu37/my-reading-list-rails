<div class="page-header">
  <h3>Book Library</h3>
</div>

<div class="books list">
  <ul>
    <% @books.each do |book| %>
        <li><%= link_to book.title, book_path(book)%> - <%= book.author_name %></li>
    <% end %>
  </ul>
</div>
