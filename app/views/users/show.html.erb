<div class="page-header">
  <h2><%= @user.name %>'s Reading List</h2>
</div>

<ul>
  <% @user.reading_list_alphabetized.each do |b| %>
    <li><%= link_to b.title, book_path(b) %></li>
  <% end %>
</ul>
